const user = {
  name: "John",
  age: 30,
};

console.log(user.name);
console.log("Hello World!");

// Types: Number, String, Boolean, Array, Object, Function, Null, Undefined,void, any, never, tuple, enum, unknown

// Syntax: let variableName: type = value;
// all the types of the variable in the typescript are in lowercase








