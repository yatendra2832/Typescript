// Enum in typescript
// enum is a special type in typescript 
// In TypeScript, an enum (short for enumeration) is a way to define a set of named constant values. It allows you to create a named set of values representing distinct categories or options.
enum SeatChoices {
    AISLE = 1,
    MIDDLE = 22,
    WINDOW =12,
    FOURTH = 4
}

const ysseat = SeatChoices.MIDDLE;
console.log(ysseat);

export{}